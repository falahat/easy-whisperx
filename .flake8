[flake8]
# Align with black (the project formatter uses line-length = 88).
# E203 and W503 conflict with black's style, so they are ignored.
max-line-length = 88
extend-ignore = E203, W503
exclude = .git,__pycache__,.venv,build,dist
